import math

# 字符串拼接
string = 'a' + 'b'
print(f"字符串拼接: 'a'+'b'='{string}'")

# 通过type确定对应元素或变量是什么类型
# python 会把不同的数据类型都定义成一个类
print(f"确定元素类型: type('a'+'b')={type('a' + 'b')}  python会把不同的数据类型都定义成class")

print(f'dir(库名)列出内置函数和变量的名称: dir(math):{dir(math)}')

# 获取模块的帮助
# help(math)

